# Copy to `frontend/.env` (Vite loads env from this folder).
# Backend transcript endpoint — must match your API origin/port.

VITE_TRANSCRIPTPARSER_API=http://127.0.0.1:8000/api/parse-transcript

# Optional: dev helper for auth (see src/auth.ts)
# VITE_SKIP_PASSWORD_AUTH=false
